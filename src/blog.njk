---
title: Blog
layout: 'base.njk'
pagination:
  data: collections.post
  size: 2
---

<div class="section container">
  <h1 class="section-title">Recent articles</h1>

  <ul role="list" class="articles__list flow">
    {%- for post in pagination.items | reverse -%}
      {% include 'article-snippet.njk' %}
    {%- endfor -%}
  </ul>


  {% if pagination.previousPageLink %}
    <a href="{{ pagination.previousPageLink }}" class="pagination-link">Previous</a>
  {% endif %}

  <span class="pagination-number">Page {{ pagination.pageNumber + 1 }}</span>

  {% if pagination.nextPageLink %}
    <a href="{{ pagination.nextPageLink }}" class="pagination-link">Next</a>
  {% endif %}
